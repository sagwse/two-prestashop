{**
 * templates/_partials/hooks/displayHome.tpl
 * v0.5.0 — Обёртка рендеринга хука displayHome.
 *
 * Сюда подключаются все модули, зарегистрированные на хук displayHome.
 * Порядок вывода управляется через:
 *   Back Office → Модули → Позиции → displayHome
 *
 * Ожидаемые модули и рекомендуемые позиции (настраивается в BO):
 *   позиция 1 — ps_categoryproducts  (сетка категорий)   ← 0.11.0: переопределение шаблона
 *   позиция 2 — ps_featuredproducts  (витрина товаров)    ← 0.11.0: переопределение шаблона
 *   позиция 3 — ps_banner            (промо-баннер)       ← по необходимости
 *   позиция 4 — ps_emailsubscription (подписка)           ← опционально
 *
 * Шаблоны модулей переопределяются в 0.11.0 через themes/mytheme/modules/
 * Этот файл — только точка вызова хука, не трогать без нужды.
 *}

{hook h='displayHome'}
