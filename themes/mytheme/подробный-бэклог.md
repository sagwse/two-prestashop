
---

## ЧТО ПРЕДСТОИТ СДЕЛАТЬ (бэклог)

> Актуально на v0.5.2 — 2026-03-18. Версии — ориентировочные, уточняются по факту выполнения.

---

### [0.6.0] — Каталог: категория, листинг, миниатюра товара

#### `templates/catalog/listing/category.tpl`
- `{extends 'layouts/layout-full-width.tpl'}`
- `{include '_partials/breadcrumb.tpl'}` — с условием `{if $breadcrumb.links|@count > 1}`
- `{include '_partials/notifications.tpl'}` — flash-сообщения PS9
- `{hook h='displaySchemaMarkup'}` — запустит `schema-category.tpl` (уже готов, v0.4.0)
- Заголовок категории: `$category.name`, описание `$category.description` (если есть)
- Изображение категории: `$category.image.large.url` (если есть), `<img loading="lazy">`
- Блок сортировки: `{hook h='displayProductListHeader'}` + выбор способа сортировки (`$listing.sort_orders`)
- Счётчик результатов: «Показано X из Y товаров»
- `{include 'catalog/listing/product-list.tpl'}` — сетка товаров
- `{include '_partials/pagination.tpl'}` — уже готов (v0.5.1), условие `pages_count > 1`
- Хуки: `displayLeftColumn` (фильтры — если `layout-left-column.tpl`), `displayRightColumn`

#### `templates/catalog/listing/product-list.tpl`
- Итерация `{foreach from=$products item='product'}`
- Для каждого: `{include file='catalog/listing/product-miniature.tpl' product=$product}`
- Bootstrap grid: `row row-cols-2 row-cols-md-3 row-cols-lg-4 g-3` (или из `$mode`)
- Пустое состояние (нет товаров): блок с текстом + CTA «В каталог»
- Переключатель вид сетки / список (если планируется) — через JS в `pages/category.js`

#### `templates/catalog/listing/product-miniature.tpl`
- Обёртка `.product-miniature` (BEM), `data-id-product`, `data-id-product-attribute`
- Изображение: `$product.cover.medium.url`, `<img loading="lazy">`, `alt="$product.name"`
- Бейджи поверх фото: скидка (`$product.discount_percentage`), Новинка, Акция — из `$product.flags[]`
- Иконка wishlist на фото: `{hook h='displayProductListFunctionalButtons' product=$product}` → blockwishlist
- ⚠️ Быстрый просмотр — **не реализовывать** (`PS_QUICK_VIEW: 0` в theme.yml)
- Название: `<a href="$product.url">$product.name</a>`, BEM `.product-miniature__name`
- Цены: текущая + перечёркнутая старая (если `$product.has_discount`) — через `$product.price` / `$product.regular_price`
- Кнопка «В корзину» (`data-button-action="add-to-cart"`) или «Выбрать характеристики» (если `$product.has_combinations`)

**Зависимости 0.6.0:** все партиалы уже готовы (v0.5.1). `schema-category.tpl` готов (v0.4.0). Только подключить и адаптировать.

---

### [0.7.0] — Карточка товара: product.tpl

#### `templates/catalog/product.tpl`
- `{extends 'layouts/layout-full-width.tpl'}`
- `{include '_partials/breadcrumb.tpl'}` (условие `|@count > 1`)
- `{include '_partials/notifications.tpl'}`
- `{hook h='displaySchemaMarkup'}` → запустит `schema-product.tpl` (уже готов, v0.4.0)
- ⚠️ **Для AggregateRating обязателен `ps_productcomments`** — задекларирован в theme.yml, `{if $product.comment_count > 0}` в schema-product.tpl уже стоит
- Опционально: FAQ аккордеон + `{include '_partials/microdata/schema-faq.tpl'}` (Вариант C — из табов/аккордеона на странице товара, если FAQ у товара есть)
- Подключить партиалы: `catalog/_partials/product-images.tpl`, `product-prices.tpl`, `product-variants.tpl`, `product-add-to-cart.tpl`
- Описание товара: краткое (`$product.description_short`) + полное (`$product.description`)
- Хуки PS9: `displayProductAdditionalInfo`, `displayReassurance`, `displayProductButtons`, `displayProductTab`, `displayProductTabContent` (ps_productcomments)
- Хук `displayProductExtraContent` — дополнительные вкладки от модулей

---

### [0.7.1] — Партиалы карточки товара

#### `templates/catalog/_partials/product-images.tpl`
- Главное фото: `$product.cover` → `large.url` или `thickbox_default.url`
- Миниатюры галереи: `{foreach from=$product.images item='image'}` с `filter id_image != cover`
- `<img loading="eager">` для первого фото (LCP-оптимизация), `loading="lazy"` для остальных
- Зум / лайтбокс — Vanilla JS в `pages/product.js` (v0.14.0)

#### `templates/catalog/_partials/product-prices.tpl`
- Обычная цена, цена со скидкой, % или сумма скидки
- `$product.has_discount`, `$product.reduction`, `$product.reduction_type` (percent / amount)
- ⚠️ Microdata `itemprop` — **не добавлять**: архитектура только JSON-LD
- Цена «по запросу» — если `$product.show_price === false`

#### `templates/catalog/_partials/product-variants.tpl`
- Цвет: swatch (кружки); другие атрибуты: radio или select — по типу группы атрибутов
- `data-product-attribute`, `data-combination-id` атрибуты для JS
- Обновление цены и главного фото при выборе — через PS9-событие `prestashop.on('updatedProduct', ...)`

#### `templates/catalog/_partials/product-add-to-cart.tpl`
- Поле количества: `<input type="number">`, min=1, max=`$product.quantity`
- Кнопка «Добавить в корзину»: `data-button-action="add-to-cart"`, `form="add-to-cart-or-refresh"`
- Состояние «Нет в наличии» → кнопка `disabled` + поясняющий текст
- Wishlist кнопка (blockwishlist)
- `{include '_partials/form-errors.tpl' field=$field}` — Режим 2 (inline под полем) для ошибок добавления

---

### [0.8.0] — Корзина и оформление заказа

#### `templates/layouts/layout-checkout.tpl` ← **НОВЫЙ layout**
- Упрощённый layout: только лого по центру + ссылка «Вернуться в магазин», без основной навигации, без footer основного
- `{include '_partials/nav.tpl'}` — ⚠️ **зарезервирован именно для этого** с v0.5.2 (в `layout-full-width.tpl` nav.tpl намеренно не вызывается)
- Блоки: `content`, `head_extra`, `body_attrs`
- Хуки: `displayAfterBodyOpeningTag`, `displayBeforeBodyClosingTag`

#### `templates/checkout/cart.tpl`
- `{extends 'layouts/layout-full-width.tpl'}` (корзина — ещё не checkout, полный layout)
- `{include '_partials/breadcrumb.tpl'}`, `{include '_partials/notifications.tpl'}`
- Таблица товаров: `{foreach from=$cart.products}` — фото, название, цена, количество (изменяемое), удалить
- Блок итого: subtotal, стоимость доставки, скидка купона, итого
- Поле промокода: если `$cart.discount_name`
- Кнопки: «Продолжить покупки» + «Оформить заказ» → `/order`
- CSS: `checkout.css` (задекларирован в theme.yml для контроллера `cart`)

#### `templates/checkout/checkout.tpl`
- `{extends 'layouts/layout-checkout.tpl'}` (упрощённый layout)
- nav.tpl подключается автоматически через `layout-checkout.tpl`
- `{include '_partials/form-errors.tpl' errors=$errors}` — Режим 1 (сводный alert над формой)
- `{include '_partials/form-errors.tpl' field=$field}` — Режим 2 (inline под полем)
- ⚠️ `id="form-errors-summary"` — **не переименовывать**: `focus()` реализуется в `pages/checkout.js` (v0.14.0)
- PS9 OnePageCheckout: шаги «Личные данные → Адрес → Доставка → Оплата»
- Хуки: `displayCheckoutSummaryTop`, `displayCheckoutSubtotalDetails`, `displayPaymentReturn`
- CSS: `checkout.css` (для контроллеров `cart` + `order`)

#### `templates/checkout/order-confirmation.tpl`
- `{extends 'layouts/layout-full-width.tpl'}`
- Блок «Заказ №{$order.reference} успешно оформлен»
- Детали: список товаров, сумма, метод доставки, адрес, статус оплаты
- JSON-LD схема `Order` — **inline в этом шаблоне** (не через отдельный `schema-*.tpl`, данные специфичны для страницы)
- Хуки: `displayOrderConfirmation`, `displayOrderConfirmation2`

---

### [0.9.0] — Личный кабинет

#### `templates/customer/login.tpl`
- Форма входа: email + password
- `{include '_partials/form-errors.tpl' errors=$errors}` — Режим 1
- `{include '_partials/form-errors.tpl' field=$field}` — Режим 2 (inline)
- Ссылки: «Забыли пароль» (`$urls.pages.password`), «Регистрация» (`$urls.pages.registration`)

#### `templates/customer/registration.tpl`
- Форма регистрации: все поля из `$customer_form` (динамическая генерация PS9)
- `{include '_partials/form-errors.tpl' errors=$errors}` — Режимы 1 и 2
- ⚠️ Структура ошибок registration отличается от checkout — оба случая уже обрабатываются в `form-errors.tpl` (v0.5.2)
- GDPR: хук `displayGDPRConsent` (psgdpr)

#### `templates/customer/my-account.tpl`
- Плитки Bootstrap grid: История заказов, Адреса, Личные данные, Вишлист, Смена пароля, Купоны
- Хуки: `displayMyAccountBlock`, `displayMyAccountBlockFooter`

**Дополнительные шаблоны customer/ (по наличию в PS9):**
- `customer/address.tpl` — форма адреса, `form-errors.tpl` оба режима
- `customer/order-history.tpl` — список заказов с пагинацией
- `customer/order-detail.tpl` — детали заказа
- `customer/password.tpl` — форма смены пароля, `form-errors.tpl`

---

### [0.10.0] — CMS страницы + ошибки

#### `templates/cms/cms.tpl`
- `{extends 'layouts/layout-full-width.tpl'}`
- `{include '_partials/breadcrumb.tpl'}`
- `{hook h='displaySchemaMarkup'}` → запустит `schema-article.tpl` (уже готов, v0.4.0)
  - Для редакционных страниц (блог, гид): `{assign var='schema_type' value='Article'}` перед подключением
  - Для контентных (Доставка, О нас, Политика): `WebPage` (дефолт, без assign)
- Опционально: блок FAQ аккордеон + `{include '_partials/microdata/schema-faq.tpl'}` (Вариант A или B)
- Хуки: `displayCMSPrintButton`, `displayCMSDisputeInformation`

#### `templates/cms/category.tpl`
- Список CMS-страниц категории: `{foreach from=$cms_categories}` + `{foreach from=$cms_pages}`
- `{include '_partials/breadcrumb.tpl'}`

#### `templates/errors/404.tpl`
- Кастомная страница 404: понятный заголовок, описание
- CTA: «На главную», «В каталог», поисковая строка (`{hook h='displaySearch'}`)
- Без `breadcrumb.tpl` (нет реального пути)
- Без JSON-LD схем (страница ошибки)

#### `templates/errors/500.tpl`
- Кастомная страница 500
- Минимальный HTML — без зависимости от layout (layout может быть недоступен при 500)
- Контакты для связи (захардкодить — PS9 переменные могут быть недоступны)

---

### [0.11.0] — Переопределения модулей

#### `themes/mytheme/modules/ps_mainmenu/`
- Аккордеон в мобильном offcanvas меню: `data-bs-toggle="collapse"` — Bootstrap без кастомного JS
- BEM-классы вместо дефолтной разметки модуля
- Горизонтальное меню на десктопе — через хук `displayNavFullWidth`

#### `themes/mytheme/modules/ps_searchbar/`
- Стилизация поля поиска под тему, BEM-классы
- Совместимость с `PS_SEARCH_AJAX: 1` (живой поиск)
- Кнопка очистки (×) — уже реализована в `theme.js` (SearchBarEnhance), только разметка

#### `themes/mytheme/modules/ps_shoppingcart/`
- Убрать лишний HTML дефолтного модуля
- Разметка под offcanvas mini-cart (`displayShoppingCartDetailed`)
- BEM: `.offcanvas-cart__item`, `.offcanvas-cart__footer`, `.offcanvas-cart__total`

#### `themes/mytheme/modules/blockwishlist/`
- BEM-классы, кастомная разметка offcanvas mini-wishlist (placement: bottom)
- Кнопка wishlist на карточке товара (сердечко поверх фото)
- ⚠️ Модуль **не входит в стандартный PS9** — установить отдельно (Marketplace / GitHub PrestaShop)
- `$urls.pages.wishlist` — проверить `|default:'#'` (уже стоит в header.tpl и footer.tpl)

#### `themes/mytheme/modules/ps_categoryproducts/`
- Сетка категорий на главной (хук `displayHome`)
- BEM-классы, Bootstrap grid
- Изображение категории + название + CTA «Смотреть»

#### `themes/mytheme/modules/ps_featuredproducts/`
- Витрина Featured товаров на главной (хук `displayHome`)
- Использовать разметку `catalog/listing/product-miniature.tpl` (v0.6.0) — или упрощённый собственный вариант, уточнить в 0.11.0

---

### [0.12.0] — Доработка theme.css

- Добавить стили для всех новых BEM-блоков, появившихся в v0.6.0–0.11.0
- Каталог: `.product-miniature__*`, `.product-list`, `.listing-header`, `.sort-bar`
- Карточка товара: `.product-images__*`, `.product-prices__*`, `.product-variants__*`, `.product-add-to-cart__*`
- Checkout: `.checkout-step__*`, `.cart-summary__*`, `.cart-product__*`
- Customer: `.login-form`, `.registration-form`, `.my-account-grid__*`
- CMS: `.cms-page__content`, `.cms-category-list`
- Errors: `.error-404__*`
- RTL-подготовка (опционально): `assets/css/rtl/theme-rtl.css`

---

### [0.13.0] — Доработка theme.js

- Проверить все 7 модулей theme.js на совместимость с новыми шаблонами (v0.6.0–0.11.0)
- Добавить обработку PS9-событий для карточки товара:
  - `prestashop.on('updatedProduct', ...)` — обновление цен и изображений при смене комбинации (variants)
  - `prestashop.on('updatedProductCombination', ...)` — синхронизация стоков
- Проверить `CartBadgeSync` — корректность обновления счётчика на новых страницах
- Проверить `SearchBarEnhance` — совместимость с переопределённым `ps_searchbar`
- `assets/js/utils/helpers.js` — утилитарные функции: `debounce`, `throttle`, `formatPrice`

---

### [0.14.0] — Страничные CSS и JS

#### CSS — `assets/css/pages/`

**`home.css`**
- Стили секций главной: Hero, USP Bar, Hook Modules, About, FAQ
- `.home-hero { background-image: url('../img/hero-bg.webp') }` — фон Hero
- BEM: `.home-hero__*`, `.home-usp__*`, `.home-about__*`, `.home-faq__*`
- ⚠️ Зафиксировано в v0.5.0: страничные стили главной перенесены именно сюда

**`category.css`**
- Специфичные стили листинга, фильтров, сортировки
- Адаптив сетки товаров (2 → 3 → 4 колонки)

**`product.css`**
- Галерея: главное фото + миниатюры
- Sticky блок добавления в корзину на десктопе (`position: sticky`)
- Стили табов отзывов, аккордеона характеристик

**`checkout.css`**
- Шаги checkout, summary, адресные формы, payment блок
- ⚠️ Один файл для двух контроллеров: `cart` и `order` (задекларировано в theme.yml v0.2.0)

#### JS — `assets/js/pages/`

**`product.js`**
- Галерея / лайтбокс — Vanilla JS, без сторонних библиотек
- Зум изображения
- Переключение комбинаций: обновление цены, главного фото, наличия
- Логика изменения количества (+ / -)

**`cart.js`**
- Обновление количества товара (AJAX)
- Удаление товара из корзины
- Применение промокода

**`checkout.js`**
- ⚠️ **`focus()` на `#form-errors-summary`** после неудачного сабмита формы
- `document.getElementById('form-errors-summary').focus()` — `tabindex="-1"` уже в разметке (v0.5.2)
- ⚠️ `id="form-errors-summary"` **не переименовывать** — зарезервировано именно для этого
- AJAX-шаги PS9 OnePageCheckout (если требует кастомизации)

**`assets/js/utils/helpers.js`**
- `debounce(fn, delay)`, `throttle(fn, delay)`
- `formatPrice(amount, currency)` — форматирование цены
- Другие shared-утилиты по мере появления

---

### [0.15.0] — SEO, микроразметка, production-ready

#### JSON-LD — финальная проверка
- Проверить все 6 схем через [Google Rich Results Test](https://search.google.com/test/rich-results)
- Проверить через [Schema.org Validator](https://validator.schema.org/)
- ⚠️ Раскомментировать `sameAs` в `schema-organization.tpl` + заполнить реальными URL соцсетей (закомментировано в v0.4.0 с пометкой «в 0.4.x»)

#### Сторонние модули — установка и настройка
- Yoast SEO for PrestaShop (или аналог): автоматизация meta title / description по шаблону
- Google Tag Manager community module
- Lazy Loading images module (или проверить нативный `loading="lazy"` — достаточен ли)

#### Pre-production контрольный список
- [ ] `twitter:site` в `head.tpl` — раскомментировать, заполнить хэндл магазина
- [ ] `h1` Hero в `index.tpl` — заменить `$shop.name` fallback на реальный ключевой запрос
- [ ] CMS page id (1–6) в `footer.tpl` и `header.tpl` — заменить на реальные id после создания страниц в BO
- [ ] `$shop.telegram` / `$shop.viber` / `$shop.whatsapp` — реализовать через кастомный модуль конфигурации (сейчас хардкод в header.tpl)
- [ ] Preload `fa-solid-900.woff2` в `head.tpl` — раскомментировать (сейчас закомментирован «до финального именования woff2»)
- [ ] Платёжные иконки Monobank / ПриватБанк в `footer.tpl` — раскомментировать `<img>` заглушки + добавить SVG в `assets/img/icons/`

---

### Ручные задачи (вне версий, выполнить отдельно)

| Файл | Параметры | Статус | Когда нужно |
|---|---|---|---|
| `assets/img/logo.svg` | Вектор, SVG | ⚠️ Создать | До любой страницы |
| `assets/img/hero-bg.webp` | 1920×900 px | ⚠️ Создать | До тестирования главной |
| `assets/img/about-home.webp` | 780×585 px (4:3) | ⚠️ Создать | До тестирования главной |
| `assets/img/og-default.jpg` | 1200×630 px | ✅ Создан (сессия 2) | — |
| `assets/img/icons/*.svg` | Visa, MC, GPay, APay, Monobank, Приват | ⚠️ Добавить | До 0.15.0 |

**Установка модулей:**
- [ ] `blockwishlist` — установить из PrestaShop Marketplace или GitHub (не входит в стандартный PS9)

**Настройка BO (перед production):**
- [ ] Создать CMS страницы: Доставка, О нас, Возврат, Контакты, Политика, Карта сайта — обновить id (1–6) в `footer.tpl`
- [ ] Настроить `ps_categoryproducts`: привязать нужные категории к хуку `displayHome`
- [ ] Настроить `ps_featuredproducts`: выбрать товары для главной страницы
- [ ] Настроить `ps_socialfollow`: реальные ссылки соцсетей магазина
- [ ] Настроить `ps_emailsubscription`: тексты, GDPR, подтверждение

---

### [1.0.0] — Production Release

**Критерии готовности:**
- [ ] Все шаблоны из ПЛАНА ВЕРСИЙ созданы и протестированы
- [ ] JSON-LD схемы прошли Google Rich Results Test без ошибок (все 6 типов)
- [ ] Core Web Vitals: LCP < 2.5s, CLS < 0.1, INP < 200ms
- [ ] Lighthouse: Performance ≥ 90 на мобиле, ≥ 95 на десктопе
- [ ] Тестирование на мобиле: iOS Safari + Android Chrome (включая safe area — реализовано через `env(safe-area-inset-bottom)`)
- [ ] Кроссбраузерность: Chrome, Firefox, Safari, Edge — последние 2 версии
- [ ] AdminBar PS9 не перекрывает контент (`z-index: 9999` — реализовано в theme.css)
- [ ] Нет JS-ошибок в консоли на всех страницах
- [ ] Все заглушки и хардкодные данные заменены на реальные
- [ ] GDPR: cookie-баннер работает, psgdpr настроен